# 2026-06-13

## Verify + push prep

- **typecheck** ✅ (shared, client, server)
- **unit:** server 357, client 389 ✅
- **lint** ✅ — додано `scripts/**/*.mjs` у ESLint (Node globals)
- **E2E** ✅ **36 passed**, 4 skipped (CI mode, fresh webServer)
  - Postgres volume після ребрендингу: `alias_postgres-data` → роль `movli` (перестворено volume)
  - Login sheet на menu блокував кліки — `gotoHome()` + dismiss у `guestJoinByCode`, `basic.spec`, `flows`, `lobby-team-builder`, `multiplayer`
- **client test:** `LazyRouteFallback.test.tsx` → `EmbeddedBootLoading` / `ScreenShell` `h-full`
- **docs:** `PROJECT_STATE.md` Postgres `movli` / volume reset note; `CHANGELOG` [Unreleased]

## Відкрито (owner)

- Manual QA @375px offline lobby + TMA desktop glass
- Profile epic мікро A–B
