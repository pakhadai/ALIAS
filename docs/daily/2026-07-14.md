# Daily — 2026-07-14

## Зроблено
- Захист від регресії orphan `alias-nginx-1` (Restarting) на VPS з Nginx Proxy Manager:
  - docs (README): канон `docker-compose.npm.yml` + `gateway`; troubleshooting orphan nginx; checklist switch/deploy; `VPS_COMPOSE_PROJECT` (`movli` vs `alias`)
  - `docker-compose.prod.yml` / `npm.yml` коментарі-попередження
  - `nginx/nginx.conf.template` з `${DOMAIN}` envsubst; stub замість hard-coded `YOUR_DOMAIN` у `nginx.conf`
  - CI `deploy-vps.yml`: warning при prod.yml і orphan `*-nginx-*` під час npm deploy
  - `.env.prod.example`, CHANGELOG [Unreleased]

## Наступне
- Merge PR → на VPS: `git pull` + npm compose up; не піднімати `alias-nginx-1`
