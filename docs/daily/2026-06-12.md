# Daily log — 2026-06-12

## TMA Desktop layout epic — ✅ complete (§0–§9)

### §9 — Docs sync + manual QA checklist

- `docs/TMA_LAYOUT.md#desktop-tma` — canon: `data-telegram-desktop`, floor 0/104, gutter off, opaque glass policy, anti-patterns, DevTools snippet.
- Owner manual QA checklist (7 steps) — pending on `tdesktop` / `macos`; mobile @375px regression rows included.
- `TMA_DESKTOP_LAYOUT_FIX_PROMPTS.md` epic status ✅; `CURRENT_FOCUS.md` updated.
- Verify: typecheck ✅, client tests **376/376** ✅.

### §8 — `@supports` fallback gap

- `@supports not (backdrop-filter)`: modal + `ui-glass-panel` opaque fallback; header/footer tint ≥90%/68% (`styles.css`, `glass.css`).

### §4 — AppHeader tgGutter off (desktop)

- `AppHeader.applyTgGutter` now requires mobile TMA: `isTelegramSession && tgChromeGutter && !isTelegramDesktopPlatform(platform)`.
- Desktop (`tdesktop`/`macos`/…) uses content rail layout — no `data-tg-gutter`, no 80px title-row inline padding.
- Vitest: 24/24 `GlassAppHeader.test.tsx` pass; mobile `ios` gutter unchanged.

### §5 — Modal backdrop padding-top (desktop)

- `html[data-telegram-app]:not([data-telegram-desktop]) .bottom-sheet-backdrop` — mobile clearance via `--tma-inset-top` (104px floor preserved).
- `html[data-telegram-desktop] .bottom-sheet-backdrop` — `padding-top: var(--tma-content-safe-top, 0px)`; EnterName / Login / QuickJoin sheets align to WebView top.

### §6 — Opaque header/footer glass (desktop)

- `glass.css`: `html[data-telegram-desktop]` — blur `::before` off; `::after` solid 92% `--ui-elevated` fill, no mask; feather height 0 on header/footer/`footer-island`.
- Mobile @375px liquid glass (48px feather, gradient masks) unchanged.
- Verify: typecheck ✅, client tests 376/376 ✅.

### §7 — Opaque modal glass (desktop)

- `styles.css`: `html[data-telegram-desktop]` — backdrop single opaque tint layer (85–90%, no blur); panel + top bar solid fill (96%/92%), pseudo blur/masks off; nested `modalNested` sheets inherit same rules.
- Mobile bottom-sheet liquid glass unchanged.
- Verify: typecheck ✅, client tests 376/376 ✅.
- Next: §8 `@supports` fallback gap.
