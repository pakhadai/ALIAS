# Daily log — 2026-06-11 (Europe/Kyiv)

## LAYOUT-001 — Phase 0: Pre-flight + baseline grep ✅

**Запит:** `@alias-steward` — сесія 0 з `docs/LAYOUT_SURFACE_UNIFICATION_PROMPTS.md` (pre-flight + baseline grep). **Без змін TSX/CSS.**

**Epic:** layout/surface/radius unification — `docs/LAYOUT_SURFACE_UNIFICATION_PROMPTS.md` (аудит 2026-06-11).

### Pre-flight (прочитано)

| Джерело | Примітка |
|---------|----------|
| `.cursor/CURRENT_FOCUS.md` | Liquid Glass ✅; Profile epic мікро A–B відкриті |
| `AUDIT_RESULTS.md` | 0 🔴 open; deferred: TMA manual QA, LobbyScreen `act()` warnings |
| `screenLayout.ts` | 6 presets: `canonical`, `narrow`, `fullPx4/6/8`, `wideMd` |
| `ScreenShell.tsx` | `layout` → `SCREEN_LAYOUT[preset].bodyClassName` + `ScreenLayoutProvider` |

### Verify (baseline)

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ green (shared, server, client) |
| `pnpm --filter @alias/client test` | ⚠️ **312/313** pass — 1 fail: `offlineGameActions.test.ts` → `TEAM_JOIN` when teams empty (lobby shells only); **не LAYOUT-001**, поза scope Phase 0 |

### Grep baseline (2026-06-11)

#### 1. `layout=` у `packages/client/src/screens/**/*.tsx`

**13 файлів** (14+ `ScreenShell` instances — `MyWordPacksScreen` має 3):

```
packages/client/src/screens/lobby/LobbyScreen.tsx          layout="fullPx4"
packages/client/src/screens/lobby/SettingsScreen.tsx       layout="canonical"
packages/client/src/screens/lobby/TeamSetupScreen.tsx      layout="fullPx8"
packages/client/src/screens/menu/JoinInputScreen.tsx       layout="wideMd"
packages/client/src/screens/menu/LobbySettingsScreen.tsx   layout="canonical"
packages/client/src/screens/menu/MyDecksScreen.tsx         layout="fullPx6" (×2 branches)
packages/client/src/screens/menu/MyWordPacksScreen.tsx     layout="canonical" (×3)
packages/client/src/screens/menu/PlayerStatsScreen.tsx     layout="narrow"
packages/client/src/screens/menu/ProfileScreen.tsx         layout="canonical"
packages/client/src/screens/menu/ProfileSettingsScreen.tsx layout="canonical"
packages/client/src/screens/menu/RulesScreen.tsx           layout="wideMd"
packages/client/src/screens/menu/StoreScreen.tsx           layout="canonical"
packages/client/src/screens/GameFlow/screens/ScoreboardScreen.tsx layout="fullPx6"
```

#### 2. `ScreenShell` consumers (unique files)

**19 файлів** у `packages/client/src/screens/**/*.tsx`.

#### 3. `ScreenShell` **без** `layout=` — **6 підтверджено** (Phase 1 scope)

| Файл | Поточний inset (audit) |
|------|------------------------|
| `menu/MenuScreen.tsx` | `contentClassName="max-w-2xl mx-auto w-full …"` |
| `GameFlow/screens/CountdownScreen.tsx` | без horizontal px у preset |
| `GameFlow/screens/GameOverScreen.tsx` | `contentClassName="px-6 …"` |
| `GameFlow/screens/PreRoundScreen.tsx` | `px-8` у className (×3 shells) |
| `GameFlow/screens/VSScreen.tsx` | `px-8` у contentClassName |
| `GameFlow/screens/RoundSummaryScreen.tsx` | `px-8` у contentClassName |

#### 4. `contentClassName=.*max-w-` (screens)

**11 унікальних рядків** (body + island footers + non-island footers):

- **Body duplicate:** `MenuScreen.tsx` — `max-w-2xl mx-auto w-full` (Phase 1)
- **Island footers (Phase 2):** `ProfileScreen`, `LobbyScreen`, `ProfileSettingsScreen`, `LobbySettingsScreen`, `StoreScreen`, `lobby/SettingsScreen`, `MyWordPacksScreen` (×2)
- **Non-island (documented):** `PreRoundScreen` `max-w-sm`, `VSScreen` `max-w-sm`

#### 5. `island contentClassName` (screens)

**10 файлів**, **14 instances** (Phase 2 SSOT):

| Файл | Pattern |
|------|---------|
| `LobbyScreen.tsx` | `max-w-sm mx-auto w-full` |
| `ProfileScreen.tsx` | `max-w-sm mx-auto w-full` |
| `ProfileSettingsScreen.tsx` | `max-w-2xl mx-auto w-full` |
| `LobbySettingsScreen.tsx` | `max-w-2xl mx-auto w-full` |
| `StoreScreen.tsx` | `max-w-2xl mx-auto w-full` |
| `lobby/SettingsScreen.tsx` | `max-w-2xl w-full` |
| `MyWordPacksScreen.tsx` | `max-w-2xl … px-6 md:px-8` (×2) |
| `MyDecksScreen.tsx` | `w-full px-6` (×2) |
| `TeamSetupScreen.tsx` | `w-full space-y-4` |
| `ScoreboardScreen.tsx` | `w-full px-6` |

#### 6. `rounded-[var(--theme-radius)]` (client TSX)

**2 файли:** `Button.tsx`, `AccentFooterCta.tsx` (Phase 3 → `.rounded-theme`).

#### 7. `currentTheme.(textMain|textSecondary)` — `screens/menu` only

**86 total** occurrences (Phase 4a baseline):

| File | Count |
|------|------:|
| `RulesModal.tsx` | 28 |
| `ProfileScreen.tsx` | 12 |
| `MyDecksScreen.tsx` | 10 |
| `StoreScreen.tsx` | 9 |
| `MyWordPacksScreen.tsx` | 8 |
| `PlayerStatsScreen.tsx` | 7 |
| `RulesScreen.tsx` | 3 |
| `JoinInputScreen.tsx` | 3 |
| `LobbySettingsScreen.tsx` | 2 |
| `ProfileSettingsScreen.tsx` | 2 |
| `MenuScreen.tsx` | 1 |
| `EnterNameSheet.tsx` | 1 |

### План ≤5 кроків (post Phase 0)

1. **Phase 1** — `layout=` на 6 missing screens; прибрати duplicate `px-*` / `max-w-*` з body `contentClassName`.
2. **Phase 2** — `footerLayout.ts` + island footer migration (14 instances).
3. **Phase 3** — `.rounded-theme` + `surfaceClasses.ts` SSOT.
4. **Phase 4a/4b** — theme legacy menu → lobby (`currentTheme.text*` baseline **86** menu).
5. **Phase 7** — `TMA_LAYOUT.md` floor 88→104 sync.

**Паралель після 0:** Phase 1 ∥ Phase 2 ∥ Phase 3.

### Наступний крок

- **Phase 2** — `@alias-steward Сесія 2: footer island SSOT` (`docs/LAYOUT_SURFACE_UNIFICATION_PROMPTS.md` §2).

---

## LAYOUT-001 — Phase 1: ScreenShell `layout=` rollout ✅

**Запит:** `@alias-steward Сесія 1: ScreenShell layout= rollout`.

### Зміни

| Файл | Preset | Прибрано з body |
|------|--------|-----------------|
| `menu/MenuScreen.tsx` | `canonical` | `max-w-2xl mx-auto w-full` |
| `GameFlow/CountdownScreen.tsx` | `fullPx6` | — |
| `GameFlow/GameOverScreen.tsx` | `fullPx6` | `px-6` |
| `GameFlow/PreRoundScreen.tsx` | `fullPx8` (×3) | `px-8` з className |
| `GameFlow/VSScreen.tsx` | `fullPx8` | `px-8` |
| `GameFlow/RoundSummaryScreen.tsx` | `fullPx8` | `px-8` |

### Тести

- `screenLayout.test.ts` — `it.each` non-empty `bodyClassName` для всіх 6 presets (+2 tests → 8 total)
- `ScreenShell.test.tsx` — існуючий merge test для `layout="canonical"` (без змін)

### Verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ |
| `pnpm --filter @alias/client test screenLayout ScreenShell` | ✅ 18/18 |
| Grep `ScreenShell…px-[468]` (duplicate px when layout) | **0** |
| `ScreenShell` без `layout=` у файлі | **0** (edge: `MyWordPacksScreen` loading branch — centered spinner, no horizontal inset) |

### Наступний крок

- **Phase 3** — `.rounded-theme` + `surfaceClasses.ts` SSOT.

---

## LAYOUT-001 — Phase 2: Footer island SSOT ✅

**Запит:** `@alias-steward Сесія 2: footer island SSOT`.

### Deliverables

| Артефакт | Деталі |
|----------|--------|
| `constants/footerLayout.ts` | `FOOTER_ISLAND_LAYOUT`, `footerIslandClassName()` — presets `narrow`, `canonical`, `fullBleed` |
| `constants/footerLayout.test.ts` | 9 tests ✅ |
| `constants/index.ts` | re-export `footerIslandClassName`, `FooterIslandPreset` |

### Міграція (14 island instances → 10 файлів)

| Preset | Файли |
|--------|-------|
| `narrow` | `LobbyScreen`, `ProfileScreen` |
| `canonical` | `ProfileSettingsScreen`, `LobbySettingsScreen`, `StoreScreen`, `lobby/SettingsScreen`, `MyWordPacksScreen` (×2) |
| `fullBleed` | `MyDecksScreen` (×2), `TeamSetupScreen` (+ `space-y-4`), `ScoreboardScreen` |

**Нотатки:** `MyWordPacksScreen` — прибрано дубль `px-6 md:px-8` з footer (body вже `layout="canonical"`). `lobby/SettingsScreen` — `mx-auto` через canonical preset (було `max-w-2xl w-full` без centering).

### Verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ |
| `pnpm --filter @alias/client test footerLayout` | ✅ 9/9 |
| Grep `island contentClassName="max-w` | **0** |
| Grep `island contentClassName` | усі через `footerIslandClassName(` |

### Наступний крок

- ~~**Phase 3**~~ → ✅ нижче.

---

## LAYOUT-001 — Phase 3: Radius utility + `surfaceClasses.ts` ✅

**Запит:** `@alias-steward Сесія 3: radius + surfaceClasses foundation`.

### Deliverables

| Артефакт | Деталі |
|----------|--------|
| `styles.css` `@theme` | `--radius-theme: var(--theme-radius)` → utility `rounded-theme` |
| `constants/surfaceClasses.ts` | `SURFACE_PANEL_CLASS`, `SURFACE_CARD_CLASS`, `SURFACE_NAV_ROW_CLASS`, `SURFACE_NAV_ACCENT_BTN_CLASS` |
| `constants/surfaceClasses.test.ts` | 4 tests ✅ |
| `profile/profileSurfaceClasses.ts` | re-export aliases + `@deprecated`; profile-only `PROFILE_LIST_CLASS`, `PROFILE_PANEL_HEADER_CLASS`, `PROFILE_STAT_CARD_CLASS` |
| `constants/index.ts` | re-export surface constants |
| `docs/UI_TOKENS.md` | § Border radius + § Surface classes |

### Мінімальна міграція

- `Button.tsx` — `rounded-theme` (було `rounded-[var(--theme-radius)]`)
- `AccentFooterCta.tsx` — те саме

### Verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ |
| `pnpm --filter @alias/client test surfaceClasses footerLayout screenLayout typography` | ✅ 33/33 |
| `test -f surfaceClasses.ts` | ✅ |
| Grep `rounded-theme` у `Button.tsx` | ✅ |

### Наступний крок

- **Phase 4a** — theme legacy menu screens (`currentTheme.text*` baseline **86**).

---

## LAYOUT-001 — Phase 4a: Theme legacy menu screens ✅

**Запит:** `@alias-steward Сесія 4a: theme legacy menu screens`.

### Зміни

| Область | Деталі |
|---------|--------|
| Shells | `currentTheme.bg` → `bg-ui-bg` (+ `transition-colors duration-500` де було) |
| Titles | `ScreenTitle` / `ModalSheetTitle` — прибрано `themeClass={currentTheme.textMain}` (default `text-ui-fg`) |
| Body copy | `currentTheme.textSecondary` → `text-ui-fg-muted` + `typographyClass.body` |
| Cards | `JoinInputScreen` / `RulesScreen` — `currentTheme.card` → `bg-ui-card border border-ui-border shadow-2xl` |
| Profile | `ProfileHero`, `ProfileStatsCards`, `ProfileBenefitsList`, `ProfileNavList`, `PlayerStatsDetailPanel` — semantic colors; props `themeTextMain/Secondary` видалено |

### Deferred (Phase 5)

`font-serif text-lg` card titles у `StoreScreen`, `MyDecksScreen`, `MyWordPacksScreen`; decorative rule numbers у `RulesScreen` / `RulesModal`.

### Grep gate (menu)

| Metric | Before | After |
|--------|--------|-------|
| `currentTheme.(textMain\|textSecondary)` | **86** | **7** (−92%) |

### Verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ |
| `pnpm --filter @alias/client test src/screens/menu` | ✅ 45/45 |

### Наступний крок

- **Phase 4b** — theme legacy lobby + in-lobby `SettingsScreen` (color classes only).

---

## LAYOUT-001 — Phase 4b: Theme legacy lobby + settings ✅

**Запит:** `@alias-steward Сесія 4b: theme legacy lobby/settings`.

### Зміни

| Файл | Деталі |
|------|--------|
| `LobbyScreen.tsx` | `bg-ui-bg`; `ScreenTitle` / `ModalSheetTitle` без `themeClass` |
| `TeamSetupScreen.tsx` | `bg-ui-bg`; `textSecondary` → `text-ui-fg-muted` (6 hits) |
| `SettingsScreen.tsx` | `bg-ui-bg`; `textMain` → `text-ui-fg` (14 hits); color-only, без restructure |
| `OnlineLobbyIntro.tsx` | redundant `theme.textMain/Secondary` прибрано де вже є `text-ui-fg(-muted)` |
| `LobbyPlayModeBar.tsx` | semantic colors |
| `TeamCard`, `PlayersSection`, `LobbyAvatarStrip` | `text-ui-fg`; `ScreenTitle` default |
| `LobbyInviteSheet`, `AddOfflinePlayerSheet` | `ModalSheetTitle` default |

### Deferred (Phase 5)

`font-serif` display titles: `TeamSetupScreen` h3, `OnlineLobbyIntro` room code, `LobbyGuestWaitingCard`.

### Grep gate (lobby, excl. tests)

| Metric | After |
|--------|-------|
| `currentTheme.(textMain\|textSecondary)` + `theme.(textMain\|textSecondary)` | **3** (deferred font-serif) |

### Verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ |
| `pnpm --filter @alias/client test lobby` | ✅ 53/53 |

### Наступний крок

- **Phase 5** — incremental surface migration (`RulesModal`, `StoreScreen`, `LobbySettingsScreen`).

---

## LAYOUT-001 — Phase 5: Surface migration (incremental) ✅

**Запит:** `@alias-steward Сесія 5: surface migration incremental`.

### Batch 1 (обов'язково)

| Файл | Заміна |
|------|--------|
| `RulesModal.tsx` | `panelBase` → `SURFACE_PANEL_CLASS`; inner cards → `SURFACE_CARD_CLASS` |
| `StoreScreen.tsx` | product/theme cards → `SURFACE_CARD_CLASS` (feature packs keep accent overrides) |
| `LobbySettingsScreen.tsx` | info/guest banners → `SURFACE_CARD_CLASS` + accent tint |
| `ProfileSettingsScreen.tsx` | section cards → `SURFACE_CARD_CLASS p-5` |

### Batch 2 (pilot)

| Файл | Заміна |
|------|--------|
| `lobby/SettingsScreen.tsx` | Game Mode block wrapper → `SURFACE_PANEL_CLASS p-6` |

### Grep gate

| Metric | After |
|--------|-------|
| `surfaceClasses` imports у `screens/**` | **5** files |
| inline `ui-glass-panel rounded` у menu TSX | **0** (via constants) |

### Verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ |
| `pnpm --filter @alias/client test RulesModal LobbySettings surfaceClasses` | ✅ 16/16 |

### Manual QA (owner)

- [ ] @375px Rules modal — glass panels, tab scroll, mode expand
- [ ] Store packs/themes cards — glass shell, owned/accent states
- [ ] Profile Settings section cards vs profile panels consistency

### Наступний крок

- **Phase 7** — verification + doc sync (grep governance, `TMA_LAYOUT.md` floor 104)

---

## LAYOUT-001 — Phase 6 (optional): Spacing tokens ✅

**Запит:** `@alias-steward Сесія 6 (optional): spacing tokens`.

### Deliverables

| Артефакт | Деталі |
|----------|--------|
| `constants/spacing.ts` | `screenBodyPy`, `sectionGap`, `sectionGapLg`, `sectionGapXl`, `stackGap` |
| `constants/spacing.test.ts` | 7 tests ✅ |
| `constants/index.ts` | re-export spacing constants |
| `docs/UI_TOKENS.md` | § Spacing (vertical rhythm) |

### Pilot migration (3 screens)

| Файл | Заміна |
|------|--------|
| `ProfileSettingsScreen.tsx` | `contentClassName` → `screenBodyPy`; inner stack → `sectionGapXl` |
| `LobbySettingsScreen.tsx` | те саме |
| `PlayerStatsScreen.tsx` | `contentClassName` + inner flex → `screenBodyPy` + `stackGap` |

### Verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ |
| `pnpm --filter @alias/client test spacing ProfileSettings LobbySettings PlayerStats` | ✅ 30/30 |

### Наступний крок

- ~~**Phase 7**~~ → ✅ нижче.

---

## LAYOUT-001 — Phase 7: Verification + doc sync ✅

**Запит:** `@alias-steward Сесія 7: verification + doc sync — docs/LAYOUT_SURFACE_UNIFICATION_PROMPTS.md`

### Grep governance (epic close)

| Gate | Target | Result |
|------|--------|--------|
| `island contentClassName="max-w` | 0 | ✅ **0** |
| `ScreenShell` без `layout=` у файлі | 0 (allowlist: `MyWordPacksScreen` loading spinner) | ✅ **0** files |
| `text-[Npx]` app UI (excl. GameFlow) | whitelist icon 18px | ✅ **1** (`ProfileSettingsScreen` Material icon) |
| `currentTheme.(textMain\|textSecondary)` menu+lobby | <20 documented | ✅ **8** (7 menu + 1 lobby deferred `font-serif`) |
| `footerIslandClassName(` consumers | all island footers | ✅ **10** files |

### Verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ green |
| `pnpm --filter @alias/client test` | ✅ **340/340** |
| `pnpm verify` (after Prettier on 20 LAYOUT files) | ✅ typecheck + lint + format:check |
| eslint | ⚠️ 1 warning — `LobbyAvatarStrip.tsx` unused `theme` (pre-existing Phase 4b) |

### Doc sync

| Файл | Зміна |
|------|-------|
| `docs/TMA_LAYOUT.md` | floor **88→104**; footer island preset table; canonical `LobbyScreen` example → `footerIslandClassName('narrow')` + `layout="fullPx4"` |
| `docs/UI_TOKENS.md` | § Screen layout presets + § Footer island presets; `--tma-content-top-floor` **104px** |
| `docs/LAYOUT_SURFACE_UNIFICATION_PROMPTS.md` | status `implemented`; epic-close metrics table |
| `.cursor/CURRENT_FOCUS.md` | LAYOUT-001 ✅ |
| `CHANGELOG.md` | [Unreleased] Phase 7 |

### Manual QA @375px (owner)

- [ ] Menu → Profile → Settings → back — header back rail aligns with body (`ScreenLayoutContext`)
- [ ] Lobby → in-lobby Settings → save footer width matches body `canonical` (`max-w-2xl`)
- [ ] Store footer trust bar width (`footerIslandClassName('canonical')`)
- [ ] Rules modal + Store cards — glass `SURFACE_*` panels after Phase 5
- [ ] PAPER_LUXE (6px) vs PREMIUM_DARK (pill) — primary buttons `rounded-theme`

### Epic close

**LAYOUT-001 Phases 0–7 ✅** — deferred: 8× font-serif card titles (TYPO Phase 8); GameFlow `text-[Npx]` display scale; Admin compact scale.

### LAYOUT-001 Micro B — game transitional layout presets ✅

**Запит:** `Мікро B: game transitional layout presets` — `docs/LAYOUT_SURFACE_UNIFICATION_PROMPTS.md` §B.

Реалізовано раніше в Phase 1; верифіковано окремою сесією (working tree, uncommitted).

| Файл | Preset | Прибрано |
|------|--------|----------|
| `PreRoundScreen.tsx` | `fullPx8` (×3 `ScreenShell`) | `px-8` з `className` |
| `VSScreen.tsx` | `fullPx8` | `px-8` з `contentClassName` |
| `RoundSummaryScreen.tsx` | `fullPx8` | `contentClassName="px-8"` |
| `GameOverScreen.tsx` | `fullPx6` | `px-6` з `contentClassName` |
| `CountdownScreen.tsx` | `fullPx6` | — (без body px) |

**Grep:** `ScreenShell` + `px-[468]` у цих 5 файлах → **0** (body inset лише через preset).

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ |
| `pnpm --filter @alias/client test screenLayout ScreenShell` | ✅ **18/18** |

**Не чіпали:** `PlayingScreen`, `ImposterScreen`, game actions, footer islands (Phase 2 scope).

### Наступний крок

- **Profile epic Micro A** — avatar preview + email overflow
- **Manual TMA @375px** — checklist вище (owner)

---

## Offline lobby parity — SOLO START_GAME + rules card ✅

**Запит:** офлайн гра — «немає гравців» при старті; немає картки правил як в онлайн.

### Зміни (код)

| Область | Деталі |
|---------|--------|
| `offlineGameActions.ts` | `prepareOfflineTeamsForStart()` + `deriveLobbyReadinessServer` на `START_GAME`; SOLO → 1 team/player; TEAMS → materialize shells |
| `LobbyRulesSummaryCard.tsx` | SSOT картки «Правила гри» (винесено з `OnlineLobbyIntro`) |
| `LobbyScreen.tsx` | OFFLINE rules card; прибрано header gear |
| `OnlineLobbyIntro.tsx` | використовує `LobbyRulesSummaryCard` |

### Тести

| Файл | Нове / оновлено |
|------|------------------|
| `LobbyRulesSummaryCard.test.tsx` | **new** — host/guest card, `rulesTimeRow` (CLASSIC/QUIZ/IMPOSTER) |
| `offlineGameActions.test.ts` | SOLO/TEAMS/QUIZ/IMPOSTER teams; readiness reject `<2 players`; TEAM_JOIN shells |
| `LobbyScreen.test.tsx` | offline + online rules card; no `lobby-header-settings`; tap → `GameState.SETTINGS` |
| `game-ui.ts` (E2E) | `openLobbySettings` — лише `lobby-settings-chips` |

### Docs

| Файл | Зміна |
|------|-------|
| `docs/LOBBY_TEAM_BUILDER.md` | offline primary path, rules card, SOLO offline START_GAME |
| `docs/TESTING_ACCEPTANCE.md` | offline SOLO invariant, rules card UX, test file map |
| `CHANGELOG.md` | [Unreleased] Fixed + Changed |
| `.cursor/CURRENT_FOCUS.md` | session wrap |

### Verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ green |
| `pnpm --filter @alias/client test lobby offlineGameActions LobbyRulesSummaryCard` | ✅ **81/81** (14 files; +7 `LobbyRulesSummaryCard`, +3 `offlineGameActions`, +1 `LobbyScreen`) |
