# Alias Master — 2026-06-07
> Швидкий огляд дня. Деталі релізів → CHANGELOG.md

## Підсумок (1–3 речення)

Session 3–4: D-4 effects refactor, audit close (0 open issues), C-1 lazy routes (main 305 KB). **Test-gap (Phases 1–8):** server 341, client 48, shared 12; `pnpm verify` green. **UI tokens + TMA session 7/7:** grep audit green, unit + E2E `@smoke` mobile 7/7; `docs/UI_TOKENS.md` → `implemented`.

### [UI tokens + TMA 7/7] — verification

- **Тип:** verification / docs
- **Область:** client, e2e, steward
- **Static grep (packages/client/src, game screens):**
  - `text-white` / `bg-white`: **0 matches**
  - admin `#111` / `#333` у `className`: **0** (лише preview fallbacks у `ThemesTab.tsx` JS — дозволений виняток)
  - `fixed bottom` без `pb-safe-*`: **0** (`FixedBottomBar`, `ClassicUI`, `ScoreboardScreen`, `PwaUpdateBanner` — усі з safe)
  - legacy tailwind colors (`champagne-gold` тощо): **0**
- **Build/verify:** `pnpm build:shared && pnpm verify` — green після Prettier `--write` на 7 файлах
- **Unit:** client **48/48**, server **341/341**, shared **12/12**
- **E2E:** `node run-playwright.mjs --grep "@smoke" --project=mobile-chrome` — **7/7 passed** (~1.4m, Pixel 5 viewport)
- **Code review (@code-reviewer):** subagent blocked (usage limit) — ручний review: no XSS in style paths, no secrets, token/safe-area conventions дотримано; `ThemesTab` preview hex — documented exception
- **Doc sync:** `UI_TOKENS.md` status `implemented`, `CURRENT_FOCUS.md`, `CHANGELOG [Unreleased]`

#### Manual TMA checklist (code-verified; device — pending owner)

| Check | Code | Device |
|-------|------|--------|
| MenuScreen — нижні кнопки не під Telegram UI | `pb-safe-bottom` на absolute footer (`MenuScreen.tsx:281`) | [ ] |
| LobbyScreen + SettingsScreen fixed CTA | `pb-safe-bottom` / `FixedBottomBar` | [ ] |
| PlayingScreen + ClassicUI footer (Correct/Skip) | `PlayingScreen` `pt-env-top pb-env-bottom` (svidomy vyniatok); `ClassicUI` `pb-safe-bottom-sm` | [ ] |
| PreRoundScreen / Scoreboard footer | `ScreenShell` + `FixedBottomBar`; Scoreboard `pb-safe-bottom-8` | [ ] |
| GameOverScreen scroll + bottom padding | `overflow-y-auto` + `pb-safe-bottom` | [ ] |
| Modal/bottom sheet над safe area | `ModalSheet` / sheets `pb-safe-bottom-8` | [x] code |
| Keyboard MenuScreen quick join | `keyboardAvoidingBottomPadding(keyboardBottomInset)` on quick-join sheet | [x] code |

## Зміни

### [Session 3] — React effects + bundle baseline
- **Тип:** refactor / perf
- **Область:** client
- **Файли:** `useDeferredOpen.ts`, `useResourceLoad.ts`, 9 screens/components, `ScoreboardScreen.tsx`, `AUDIT_RESULTS.md`, `CHANGELOG.md`
- **Що:** D-4 resolved; deferred comments on socket/TMA/auth bootstrap; C-1 baseline recorded.
- **Тести:** typecheck 0; server 208/208; client 20/20; build green.

### [Session 4] — Audit close + bundle guard
- **Тип:** docs
- **Область:** steward
- **Файли:** `AUDIT_RESULTS.md`, `.cursor/CURRENT_FOCUS.md`, `AGENT_BRIEF.md`
- **Що:** A-1, B-1, C-2, D-5, F-1 → ✅ ACCEPTED/VERIFIED; bundle re-check 2026-06-07 identical to baseline (main 500.0 KB).
- **Код:** без змін поведінки.

### [Test-gap] — Phases 1–8 (commit `db1ecf5`)
- **Тип:** test / docs
- **Область:** server, client, shared, e2e
- **Що:** G-1 authorizeGameAction (42) → G-8 E2E specs; REST routes + socket integration; coverage `include` розширено; thresholds 67/71/89%.
- **Baseline:** server **341**, client **44**, shared **12**; `pnpm verify` green.
- **E2E:** `smoke-round.spec.ts`, `core-acceptance.spec.ts`, `helpers/game-ui.ts` — локально потрібен Postgres.
- **Doc sync:** `AGENT_BRIEF.md` (post test-gap snapshot + optional backlog).

### [E2E hardening] — offline TEAM_JOIN + lobby locators
- **Тип:** fix / test
- **Область:** client, e2e
- **Файли:** `offlineGameActions.ts`, `game-ui.ts`, `core-acceptance.spec.ts`, `TeamCard.tsx`, `SettingsScreen.tsx`, `ImposterScreen.tsx`, `translations.ts`
- **Що:** materialize offline teams; `expectLobbyReadyToStart`; multilingual locators; IMPOSTER `imposter-reveal-cta`; multiplayer → shared helpers.
- **Тести:** typecheck 0; offlineGameActions 12/12; locator tests 8/8; E2E local blocked (no Postgres).

## Відкриті питання

- Owner: `noUncheckedIndexedAccess` — один PR чи поетапно (лише в AGENT_BRIEF ToDo).
- E2E локально: `docker compose up -d postgres redis` перед `pnpm test:e2e`.
- Post test-gap backlog (routes ~18–40%, socketHandlers ~48%) — лише при зміні відповідних API/relay.

## Doc sync
- [x] AUDIT_RESULTS (Block G G-1…G-8)
- [x] CURRENT_FOCUS (session 7/7 closed)
- [x] AGENT_BRIEF (test-gap snapshot)
- [x] CHANGELOG [Unreleased] (7/7 verification)
- [x] docs/daily/2026-06-07
- [x] docs/UI_TOKENS.md (status: implemented)
