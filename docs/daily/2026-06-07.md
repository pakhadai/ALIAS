# MOVLI Master — 2026-06-07
> Швидкий огляд дня. Деталі релізів → CHANGELOG.md

## Підсумок (1–3 речення)

Session 3–4: ModalSheet Phases 0–5 green. **TYPO-001 Phases 1–6:** epic implemented; Phase 7 QA pending.

### [TYPO-001 Phase 6] — font + governance

- **Тип:** changed / docs
- **index.html:** Merriweather removed; Playfair 400/700/900
- **Rule:** `.cursor/rules/08-typography.mdc`
- **Sweep:** app UI `text-[Npx]` → 0 (GameFlow + icon glyphs whitelisted)
- **Verify:** `pnpm verify` green

### [TYPO-001 Phase 5] — system messages

- **Тип:** changed
- **Helpers:** `systemBannerClass`, `systemStatusClass`
- **Файли:** ConnectionStatusBanner, PwaUpdateBanner, MenuScreen error strip, LobbyScreen guest connection UI, TelegramAuthLoadingScreen, App TMA auth error, SentryErrorFallback, StoreScreen purchase banner
- **Правило:** banner/status → `text-ui-system`; пояснення → `text-ui-body`; toast без змін
- **Verify:** client **125/125**, typecheck green

### [TYPO-001 Phase 4] — labels & captions

- **Тип:** changed
- **Helpers:** `labelSectionClass`, `labelSectionTitleClass`, `formLabelClass` in `constants/typography.ts`
- **Файли:** ~27 (menu, lobby, admin, modals, store, `Button.tsx`, `Shared.tsx` Logo subtitle)
- **grep:** `text-[7-11px]` in-scope → **0**; escapes: `GameFlow/*`, `PwaUpdateBanner`, `SentryErrorFallback`
- **Verify:** client **123/123**, typecheck green

### [TYPO-001 Phase 3b] — body text menu + lobby

- **Тип:** changed
- **Файли:** MenuScreen, EnterNameSheet, ProfileScreen/Settings, PlayerStatsScreen, OnlineLobbyIntro, TeamCard, UnassignedPool, LobbyGuestWaitingCard, LobbyPlayModeBar, AssignPlayerSheet, LobbyInviteSheet, PlayersSection
- **grep:** `text-[13px]` у `screens/menu` + `screens/lobby/components` → **0**
- **Verify:** client **123/123**, typecheck green
- **Залишки:** CTA `text-sm uppercase` на MenuScreen кнопках (Phase 4); LobbyScreen connection strips (Phase 5)

### [TYPO-001 Phase 3a] — body text у modals + shared

- **Тип:** changed
- **Tokens:** `text-ui-body-input` (16px) для input/textarea
- **Файли:** `ConfirmationModal`, `LoginModal`, `RulesModal`, `AppSettingsModal`, `CustomDeckModal`, `QuickBuyModal`, `Shared` (toast), `App.tsx`
- **Verify:** client **123/123**, typecheck green
- **Не чіпали:** section labels (Phase 4), Stars CTA button, tab chips у RulesModal

### [TYPO-001 Phase 2] — primitives & screen headings

- **Тип:** added / changed
- **Область:** client components + menu/admin/lobby screens
- **Файли:** `ScreenTitle.tsx`, `Shared.tsx` (`modalSheetTitleClass`), menu screens (Profile, Store, Rules, …), `AdminApp`, lobby (`PlayersSection`, `LobbyAvatarStrip`, `LobbyScreen`), `LoginModal`
- **Build/verify:** client **123/123**; `pnpm typecheck` green
- **grep menu/admin headings:** `font-serif text-(2xl|3xl|[26px])` → **0** (admin `StatsTab` stat values — out of scope)

### [TYPO-001 Phase 1] — typography token foundation

- **Тип:** added / foundation
- **Область:** client CSS + constants + docs
- **Файли:** `styles.css` (`--ui-text-*`, `@theme` font-size/leading/tracking), `constants/typography.ts`, `typography.test.ts`, `UI_TOKENS.md`, `TYPOGRAPHY_UNIFICATION.md`
- **Tokens:** `text-ui-heading` (24px), `text-ui-body` (14px), `text-ui-label` (10px), `text-ui-system` (12px)
- **Build/verify:** `pnpm typecheck` green; typography unit **5/5**
- **Scope:** no TSX consumer migration (Phase 2+)

### [ModalSheet Phase 5] — verification + governance

- **Тип:** verification / docs / fix
- **Область:** client modals, steward
- **grep guards:** `panelClassName=` consumers **0**; `panelVariant="card"` **0**; modal `max-h-[*dvh]` **0**; `pb-safe-bottom` on modal backdrop/panel **0**
- **backdrop-blur на modal consumers:** **0** (винятки поза scope: `AppSettingsModal` theme lock overlay, `Shared` milestone popup, game headers/banners)
- **Build/verify:** `pnpm typecheck` green; `pnpm build:shared` skipped (shared untouched)
- **Unit:** client **114/114** (targeted modal suites 34/34)
- **E2E:** `@smoke|@core` mobile-chrome — **8/15 passed**, 3 failed enter-name→lobby (needs fresh client in docker / investigate)
- **Fix:** `EnterNameScreen` — duplicate submit (`form` + `onClick`)
- **Doc sync:** `TMA_LAYOUT.md` Phase 5 table, `CURRENT_FOCUS.md`, `07-modals.mdc`, `CHANGELOG [Unreleased]`

### [UI tokens + TMA 7/7] — verification

- **Тип:** verification / docs
- **Область:** client, e2e, steward
- **Static grep (packages/client/src, game screens):**
  - `text-white` / `bg-white`: **0 matches**
  - admin `#111` / `#333` у `className`: **0** (лише preview fallbacks у `ThemesTab.tsx` JS — дозволений виняток)
  - `fixed bottom` без `pb-safe-*`: **0** (`FixedBottomBar`, `ClassicUI`, `ScoreboardScreen`, `PwaUpdateBanner` — усі з safe)
  - legacy tailwind colors (`champagne-gold` тощо): **0**
- **Build/verify:** `pnpm build:shared && pnpm verify` — green після Prettier `--write` на 7 файлах
- **Unit:** client **48/48**, server **341/341**, shared **12/12**
- **E2E:** `node run-playwright.mjs --grep "@smoke" --project=mobile-chrome` — **7/7 passed** (~1.4m, Pixel 5 viewport)
- **Code review (@code-reviewer):** subagent blocked (usage limit) — ручний review: no XSS in style paths, no secrets, token/safe-area conventions дотримано; `ThemesTab` preview hex — documented exception
- **Doc sync:** `UI_TOKENS.md` status `implemented`, `CURRENT_FOCUS.md`, `CHANGELOG [Unreleased]`

### [ModalSheet Phase 2 presets API] — size canon centralization
- **Тип:** changed / API
- **Файли:** `ModalSheet.presets.ts` (new), `ModalSheet.tsx`, `ModalSheet.test.tsx`, `ConfirmationModal.tsx`, `TMA_LAYOUT.md`, `UI_TOKENS.md`
- **Що:** padding + default `maxWidth` per `size`; `ModalSheetBody`/`Footer`; `paddedContent` deprecated JSDoc; `compact` `px-5` + `max-w-sm`; tests 8/8; `pnpm typecheck` green

### [ModalSheet Phase 4] — remove `panelVariant`
- **Тип:** removed / refactor
- **Файли:** `ModalSheet.tsx`, `Shared.tsx`, `styles.css`, `ModalSheet.test.tsx`
- **Що:** видалено `panelVariant`, `BottomSheetPanelVariant`, `.bottom-sheet-panel--card`; `bottomSheetPanelClass(extra, size)`

### [ModalSheet Phase 3] — simple consumers → size presets
- **Тип:** changed / UI
- **Файли:** `QuickJoinSheet.tsx`, `LobbyInviteSheet.tsx`, `MenuScreen.tsx`, `LobbyScreen.tsx`, `QuickBuyModal.tsx`, `AssignPlayerSheet.tsx`, `LogoutConfirmBottomSheet.tsx`
- **Що:** confirm/invite/quick-join/fullscreen-hint/QR → `size="compact"`; add-player/QuickBuy/AssignPlayer → `default`; redundant `maxWidth`/`paddedContent`/`contentClassName` прибрано; QuickBuy pay → `Button primary`; AssignPlayer cancel → `Button ghost`
- **grep:** `panelClassName` у Phase 3 targets — 0
- **Тести:** LobbyScreen 9/9, MenuScreen 8/8, ModalSheet 8/8, QuickBuy 3/3, LogoutConfirm 2/2; typecheck green

### [ModalSheet Phase 3] — CTA unification
- **Тип:** changed / UI
- **Файли:** `LogoutConfirmBottomSheet.tsx`, `AdminApp.tsx`, `LogoutConfirmBottomSheet.test.tsx`
- **Що:** danger/primary confirm + ghost cancel (як `ConfirmationModal`); admin confirm vertical fullWidth stack
- **Тести:** logout confirm 2/2; typecheck green

### [ModalSheet Phase 2] — consumer migration
- **Тип:** changed / UI
- **Область:** 16 `ModalSheet` call sites
- **Що:** `size` на всіх consumers; `LoginModal` card→tall edge-to-edge; прибрано `panelClassName` padding / `max-h-[90|92dvh]`; tall flex column CSS hook
- **Наступне:** Phase 3 — CTA unification

### [ModalSheet Phase 1b] — `size` API
- **Тип:** changed / UI
- **Область:** `ModalSheet.tsx`, `Shared.tsx`, `styles.css`
- **Що:** `size="compact"|"default"|"tall"`; built-in padding (`px-8 text-center` / `px-5`); `bottomSheetPanelClass` size modifiers; `data-sheet-scroll` on tall; `panelVariant` deprecated
- **Тести:** `ModalSheet.test.tsx` (5); fix `LobbyStartPanel.test.tsx` (`items` vs `checks`)
- **Наступне:** Phase 2 — consumer migration

### [ModalSheet Phase 1a] — visual foundation (CSS primitives)
- **Тип:** changed / UI
- **Область:** client ModalSheet primitives
- **Файли:** `packages/client/src/styles.css`, `docs/UI_TOKENS.md`
- **Що:** backdrop glass — blur 36px, bottom scrim tint, softer mask; panel frosted glass (inset `::before`/`::after`, lobby-start-glass pattern); `.bottom-sheet-panel--sheet` `height: auto`; reduced-transparency opaque fallback
- **Не чіпали:** consumers, `ModalSheet.tsx`, `@movli/shared`
- **Наступне:** Phase 1b — `size` prop

### [ModalSheet Phase 0] — audit & target contract
- **Тип:** docs / steward
- **Область:** client UI governance (без TSX)
- **Файли:** `docs/TMA_LAYOUT.md`, `docs/UI_TOKENS.md`, `.cursor/rules/07-modals.mdc`, `.cursor/CURRENT_FOCUS.md`
- **Що:** grep-матриця **16** `ModalSheet` consumers; target `size` presets (`compact`/`default`/`tall`); outlier priorities (`LoginModal` card, `90/92dvh`, px drift, raw CTA); виправлено doc drift `pb-env-bottom` → `pb-modal-bottom` у ModalSheet секції
- **Наступне:** Phase 1 — `size` prop у `ModalSheet.tsx` + backdrop tokens

### [Lobby Phase 3] — polish (invite sheet, haptics, a11y)
- **Тип:** feat / UX
- **Область:** client lobby
- **Файли:** `LobbyInviteSheet.tsx`, `OnlineLobbyIntro.tsx`, `LobbyScreen.tsx`, lobby components, `haptics.ts`, `translations.ts`
- **Що:** extract invite sheet (4 options, nested QR); TMA haptics; typography/token/a11y pass
- **Тести:** lobby vitest 18/18; `pnpm typecheck` green

#### Manual TMA checklist (code-verified; device — pending owner)

| Check | Code | Device |
|-------|------|--------|
| MenuScreen — нижні кнопки не під Telegram UI | `pb-safe-bottom` на absolute footer (`MenuScreen.tsx:281`) | [ ] |
| LobbyScreen + SettingsScreen fixed CTA | `pb-safe-bottom` / `FixedBottomBar` | [ ] |
| PlayingScreen + ClassicUI footer (Correct/Skip) | `PlayingScreen` `pt-env-top pb-env-bottom` (svidomy vyniatok); `ClassicUI` `pb-safe-bottom-sm` | [ ] |
| PreRoundScreen / Scoreboard footer | `ScreenShell` + `FixedBottomBar`; Scoreboard `pb-safe-bottom-8` | [ ] |
| GameOverScreen scroll + bottom padding | `overflow-y-auto` + `pb-safe-bottom` | [ ] |
| Modal/bottom sheet над safe area | `ModalSheet` / sheets `pb-safe-bottom-8` | [x] code |
| Keyboard MenuScreen quick join | `keyboardAvoidingBottomPadding(keyboardBottomInset)` on quick-join sheet | [x] code |

## Зміни

### [Session 3] — React effects + bundle baseline
- **Тип:** refactor / perf
- **Область:** client
- **Файли:** `useDeferredOpen.ts`, `useResourceLoad.ts`, 9 screens/components, `ScoreboardScreen.tsx`, `AUDIT_RESULTS.md`, `CHANGELOG.md`
- **Що:** D-4 resolved; deferred comments on socket/TMA/auth bootstrap; C-1 baseline recorded.
- **Тести:** typecheck 0; server 208/208; client 20/20; build green.

### [Session 4] — Audit close + bundle guard
- **Тип:** docs
- **Область:** steward
- **Файли:** `AUDIT_RESULTS.md`, `.cursor/CURRENT_FOCUS.md`, `AGENT_BRIEF.md`
- **Що:** A-1, B-1, C-2, D-5, F-1 → ✅ ACCEPTED/VERIFIED; bundle re-check 2026-06-07 identical to baseline (main 500.0 KB).
- **Код:** без змін поведінки.

### [Test-gap] — Phases 1–8 (commit `db1ecf5`)
- **Тип:** test / docs
- **Область:** server, client, shared, e2e
- **Що:** G-1 authorizeGameAction (42) → G-8 E2E specs; REST routes + socket integration; coverage `include` розширено; thresholds 67/71/89%.
- **Baseline:** server **341**, client **44**, shared **12**; `pnpm verify` green.
- **E2E:** `smoke-round.spec.ts`, `core-acceptance.spec.ts`, `helpers/game-ui.ts` — локально потрібен Postgres.
- **Doc sync:** `AGENT_BRIEF.md` (post test-gap snapshot + optional backlog).

### [E2E hardening] — offline TEAM_JOIN + lobby locators
- **Тип:** fix / test
- **Область:** client, e2e
- **Файли:** `offlineGameActions.ts`, `game-ui.ts`, `core-acceptance.spec.ts`, `TeamCard.tsx`, `SettingsScreen.tsx`, `ImposterScreen.tsx`, `translations.ts`
- **Що:** materialize offline teams; `expectLobbyReadyToStart`; multilingual locators; IMPOSTER `imposter-reveal-cta`; multiplayer → shared helpers.
- **Тести:** typecheck 0; offlineGameActions 12/12; locator tests 8/8; E2E local blocked (no Postgres).

### [Lobby play format animation] — guest exit slide
- **Тип:** feat / UX
- **Область:** client lobby
- **Файли:** `LobbyPlayModeBarSlot.tsx`, `LobbyScreen.tsx`, `styles.css`, `useBottomSheetPresence.ts`, `LobbyPlayModeBarSlot.test.tsx`, `LobbyScreen.test.tsx`
- **Що:** анімація блоку «Формат гри» — в’їзд зверху, виїзд вгору після `TEAM_JOIN` (лише guest); height-measured collapse (clip-safe у scroll TMA); 520 ms; host bar лишається
- **Причина ітерацій:** CSS `transition` + `grid-rows` + `translateY` обрізались `overflow-y-auto`; синхронний `visible=false` не давав transition; фінал — inline height + lift + double rAF на exit
- **Тести:** `LobbyPlayModeBarSlot` 2/2; `LobbyScreen` +2 (guest bar visible/hidden); device TMA — pending owner

## Doc sync (play format animation session)
- [x] `.cursor/CURRENT_FOCUS.md`
- [x] `CHANGELOG.md` [Unreleased]
- [x] `docs/LOBBY_TEAM_BUILDER.md`
- [x] `docs/daily/2026-06-07.md`

## Відкриті питання

- Owner: `noUncheckedIndexedAccess` — один PR чи поетапно (лише в AGENT_BRIEF ToDo).
- E2E локально: `docker compose up -d postgres redis` перед `pnpm test:e2e`.
- Post test-gap backlog (routes ~18–40%, socketHandlers ~48%) — лише при зміні відповідних API/relay.

### [ModalSheet edge-to-edge]

- **Тип:** fix / UX
- **Область:** client modals, TMA layout docs
- **Що:** backdrop без `padding-bottom`; панель від низу екрана; safe area лише `pb-env-bottom` у контенті; keyboard lift = `keyboardPx` без додаткового `max(1.5rem, …)`
- **Файли:** `styles.css`, `ModalSheet.tsx`, `useVisualViewportBottomInset.ts`, modal consumers; `docs/TMA_LAYOUT.md`, `docs/UI_TOKENS.md`
- **Тести:** `useVisualViewportBottomInset.test.ts` 6/6

## Doc sync
- [x] AUDIT_RESULTS (Block G G-1…G-8)
- [x] CURRENT_FOCUS (session 7/7 closed)
- [x] AGENT_BRIEF (test-gap snapshot)
- [x] CHANGELOG [Unreleased] (7/7 verification)
- [x] docs/daily/2026-06-07
- [x] docs/UI_TOKENS.md (status: implemented)

### [ModalSheet Phase 4] — tall escape hatches
- **Тип:** changed / refactor
- **Файли:** `LoginModal`, `RulesModal`, `AppSettingsModal`, `CustomDeckModal`, `PlayingPauseOverlay`, `EnterNameScreen`, `ImposterScreen`, `styles.css`, smoke tests
- **Що:** tall→`ModalSheetBody`/`Footer`; Login→`default`; pause shadow removed; EnterName loading→canonical backdrop; Imposter→`ConfirmationModal`; tall CSS `overflow:hidden` split scroll
