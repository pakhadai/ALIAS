# Daily log — 2026-06-10 (Europe/Kyiv)

## Profile / Lobby Settings epic — Session 0 (pre-flight)

**Запит:** `@alias-steward` — pre-flight + verify baseline (`docs/PROFILE_LOBBY_SETTINGS_IMPROVEMENT_PROMPTS.md` §0).

### Baseline verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ green (4 packages) |
| `pnpm --filter @alias/client test` | ✅ **244/244** (50 files) |

### Файли epic (інвентаризація)

| Файл | Роль |
|------|------|
| `packages/client/src/screens/menu/ProfileScreen.tsx` | Hero, stats, nav |
| `packages/client/src/screens/menu/ProfileSettingsScreen.tsx` | Avatar, ім'я, push, PWA |
| `packages/client/src/screens/menu/LobbySettingsScreen.tsx` | Дефолти нових кімнат (`saveLobbySettings`) |
| `packages/client/src/screens/lobby/SettingsScreen.tsx` | Live sync поточної кімнати (host) |
| `packages/client/src/screens/menu/profile/ProfileNavList.tsx` | Nav секції profile |
| `packages/client/src/screens/menu/PlayerStatsScreen.tsx` | Reference: theme bg + dots |
| `packages/client/src/constants/translations.ts` | i18n UA/DE/EN |
| `packages/client/src/services/api.ts` | `fetchLobbySettings` / `saveLobbySettings` (+ 30s cache) |

### Вже зроблено в repo

- TMA Phase 3a: `ProfileSettingsScreen`, `LobbySettingsScreen` → `ScreenShell` + `AppHeader` + `FixedBottomBar`
- `ProfileScreen` / `PlayerStatsScreen`: `useT()`, `currentTheme.bg`, dot pattern, glass chrome
- `ProfileNavList`: i18n labels з `ProfileScreen` (props `labels.*`)
- `LobbySettingsScreen.test.tsx`: 1 тест (load + save filtered fields)
- `SettingsScreen`: еталон — `LOBBY_LANG_FLAG`, `t.cat_*`, `role="switch"`, haptics

### Відкриті пункти аудиту (epic)

| # | Проблема | Сесія | Статус |
|---|----------|-------|--------|
| 1 | Hardcoded UA в ProfileSettings + LobbySettings | 1 | ✅ |
| 2 | Незрозумілі «два settings» | 2 | ✅ |
| 3 | Silent save errors (`catch` ковтає) | 3 | ✅ |
| 4 | `bg-ui-bg` vs `currentTheme.bg` + dots | 4 | ✅ |
| 5 | Raw `Category.*`, no flags, weak toggle | 5 | ✅ |
| 6 | Reset без confirm + не factory defaults | 6 | ✅ |
| 7 | Duplicate stats у `authBenefits` + guest store | 7 | ✅ |
| 8 | Shared primitives + dirty guard | 8 | 🔲 |
| A | Avatar preview / email overflow | A | 🔲 |
| B | SettingsScreen header → `ScreenTitle` | B | 🔲 |
| C | Guest path до lobby defaults | C | 🔲 |

### Рекомендований порядок сесій 1–8

```
1 (i18n P0) → 2 (copy, після 1) → 3 (errors P0, паралельно з 1–2 OK)
→ 4 (visual P1) → 5 (lobby polish, після 1) → 6 (reset, після 3)
→ 7 (profile dedup, незалежно) → 8 (shared + dirty, після 4+5)
```

Мікро A–C — за контекстом після P0 або паралельно з P1.

### Наступний крок (post Session 0)

**Сесія 4** — visual shell unify ProfileSettings + LobbySettings.

---

## Profile / Lobby Settings epic — Session 1 (i18n)

**Запит:** `@alias-steward` — i18n `ProfileSettingsScreen` + `LobbySettingsScreen` (§1).

### Зроблено

- `ProfileSettingsScreen.tsx` — `useT()`; 0 hardcoded UA (avatar, name, skip-name toggle, account, push/PWA)
- `LobbySettingsScreen.tsx` — `useT()`; категорії через `t.cat_*`; time labels через `timeMinShort` / `timeSecShort`
- `translations.ts` — 24 нових ключів × UA/DE/EN (`saved`, `reset`, `profileSettings*`, `lobbyDefaultsSave`, `lobbyWordLanguage`, …)
- `LobbySettingsScreen.test.tsx` — mock `useT` (EN strings), assertions через role/name

### Verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ green |
| `pnpm --filter @alias/client test` | ✅ **245/245** |
| `rg` UA у tsx (поза `t.*`) | ✅ 0 matches |

---

## Profile / Lobby Settings epic — Session 2 (copy / explain)

**Запит:** `@alias-steward` — пояснення дефолти лоббі vs налаштування кімнати (§2).

### Зроблено

- `LobbySettingsScreen.tsx` — info banner (`lobbyDefaultsInfoBanner`) з `systemStatusClass`, accent-tinted border (як `PlayerStatsScreen` guest banner без CTA)
- `ProfileNavList.tsx` — subtitle під «Налаштування лоббі» (`profileNavLobbySettingsSub`), патерн як `unlockPacksSub`
- `ProfileScreen.tsx` — передача `lobbySettingsSub` у labels
- `translations.ts` — 2 ключі × UA/DE/EN
- Tests: `LobbySettingsScreen.test.tsx` (banner visible), `ProfileNavList.test.tsx` (subtitle)

### Verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ green |
| `pnpm --filter @alias/client test` | ✅ **247/247** |

### Наступний крок

**Сесія 4** — visual shell unify (`currentTheme.bg` + dots, footer CTA).

---

## Profile / Lobby Settings epic — Session 3 (error feedback)

**Запит:** `@alias-steward` — error feedback при save профілю/lobby defaults (§3).

### Зроблено

- `ProfileSettingsScreen.tsx` — `showNotification(t.profileSettingsSaveFailed, 'error')` у catch; `finally` для `setSaving`
- `LobbySettingsScreen.tsx` — `showNotification(t.lobbyDefaultsSaveFailed, 'error')` у catch
- `translations.ts` — 2 ключі × UA/DE/EN
- `ProfileSettingsScreen.test.tsx` — fail + success paths
- `LobbySettingsScreen.test.tsx` — fail path + `showNotification` mock

### Verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ green |
| `pnpm --filter @alias/client test` | ✅ **250/250** |

### Наступний крок

**Сесія 5** — LobbySettings UI polish (категорії, прапори, toggles, haptics).

---

## Profile / Lobby Settings epic — Session 4 (visual shell)

**Запит:** `@alias-steward` — візуальна уніфікація settings screens з profile flow (§4).

### Зроблено

- `ProfileSettingsScreen.tsx` — `currentTheme.bg` + dot pattern; glass `FixedBottomBar`; `Button` size xl fullWidth замість raw `<button>`
- `LobbySettingsScreen.tsx` — те саме; `ScreenTitle` без змін (heading)
- TMA: `ScreenShell` + `AppHeader` збережено; `SettingsScreen` (in-lobby) — out of scope

### Verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ green |
| `pnpm --filter @alias/client test` | ✅ **250/250** |

### Наступний крок

**Сесія 5** — LobbySettings UI polish (категорії, прапори, toggles, haptics).

---

## Profile / Lobby Settings epic — Session 5 (LobbySettings UI polish)

**Запит:** `@alias-steward` — категорії, прапори, toggles, haptics (§5).

### Зроблено

- `LobbySettingsScreen.tsx` — `LOBBY_LANG_FLAG` на language chips (як `SettingsScreen`)
- Категорії: Lucide icons (`Sparkles`, `Utensils`, …) + `t.cat_*` labels; `aria-pressed` / `aria-label`
- Skip penalty: `role="switch"`, `aria-checked`, enabled/disabled label (патерн in-lobby)
- Sliders: `vibrate(HAPTIC.nav)` на step change (round time, discussion time, score to win)
- `LobbySettingsScreen.test.tsx` — flags, category labels (не raw enum), switch a11y

### Verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ green |
| `pnpm --filter @alias/client test` | ✅ **251/251** |

### Наступний крок

**Сесія 6** — Reset lobby defaults (confirm + factory semantics).

---

## Profile / Lobby Settings epic — Session 6 (Reset lobby defaults)

**Запит:** `@alias-steward` — confirm + factory semantics (§6).

### Зроблено

- `ConfirmationModal` перед reset; i18n `lobbyDefaultsResetTitle/Message/Confirm/Failed` (UA/DE/EN)
- `gameReducer.ts` — `isEmptySavedLobbySettings`, `applyFactoryLobbyDefaults` (syncable fields з `initialState`, device prefs зберігаються)
- `handleResetConfirm`: `saveLobbySettings({})` → `fetchLobbySettings` → factory UI
- Deep-merge `general` / `mode` при load (partial remote не затирає skipPenalty тощо)
- Vitest: cancel без save; success path (40 → 30 factory score)

### Verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ green |
| `pnpm --filter @alias/client test` | ✅ **255/255** |

### Наступний крок

**Сесія 7** — ProfileScreen dedup stats + guest/store nav.

---

## Profile / Lobby Settings epic — Session 7 (ProfileScreen dedup)

**Запит:** `@alias-steward` — dedup stats + guest/store nav (§7).

### Зроблено

- `ProfileScreen.tsx` — auth: прибрано рядок 📊 з `authBenefits` (stats лише в `ProfileStatsCards` + tap → `PLAYER_STATS`)
- Guest: прибрано 🛍️ store row з `guestBenefits`; залишено одну CTA «Browse store» (`profileGuestBrowseStore`)
- `LobbySettingsScreen.tsx` — `mergePartialLobbySettings` helper (typecheck fix для partial remote merge)

### Verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ green |
| `pnpm --filter @alias/client test` | ✅ **255/255** |

### Наступний крок

**Сесія 8** — shared settings primitives + unsaved changes guard.

---

## Profile / Lobby Settings epic — Session 8 (shared primitives + dirty guard)

**Запит:** `@architect @alias-steward` — §8.

### Part A — shared components (`components/Settings/`)

- `SettingsToggle`, `SettingsSlider`, `LanguageChipRow`, `CategoryChipGrid`
- `categoryUtils`, `LOBBY_LANG_FLAG`, `lobbySettingsCompare`
- `LobbySettingsScreen` — повна міграція на primitives
- `SettingsScreen` — incremental DRY (categories, translation/pack language chips, skip penalty toggle)

### Part B — unsaved changes

- `BackNavigationGuardProvider` (App.tsx) + `useUnsavedChangesGuard`
- `UnsavedChangesModal` (Save / Discard / Stay)
- `ProfileSettingsScreen` + `LobbySettingsScreen`: dirty baseline, guarded `AppHeader` back
- `useTelegramBackButton` — `runGuardedNavigation` для TMA BackButton
- i18n `settingsUnsavedTitle|Message|Save|Discard|Stay` (UA/DE/EN)

### Verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ green |
| `pnpm --filter @alias/client test` | ✅ **262/264** (2 flaky `QuickBuyModal` — pre-existing) |

### Наступний крок

**Мікро A–B** — avatar/email, in-lobby header.

---

## Micro C — guest lobby defaults (localStorage A)

**Рішення:** варіант A — `localStorage` для anonymous; auth лишає `saveLobbySettings` API.

### Зміни

- `lib/guestLobbyDefaults.ts` — load/save/clear + `mergeSavedLobbyDefaultsIntoSettings`
- `api.ts` — `getTokenAuthType` / `isAnonymousSession` (JWT decode для routing)
- `LobbySettingsScreen` — guest: localStorage persist/reset, sign-in banner; auth: без змін
- `ProfileScreen` — nav «Налаштування лоббі» для гостя
- `GameContext.createNewRoom` — guest merge з localStorage; auth — `fetchLobbySettings`
- i18n `lobbyDefaultsGuestBannerBody` (UA/DE/EN)

### Verify

| Check | Result |
|-------|--------|
| `pnpm typecheck` | ✅ green |
| Vitest (guestLobbyDefaults + LobbySettingsScreen) | ✅ 11/11 |
