# Alias Master — 2026-06-08

**TYPO-001 Session A:** 5 screens — inputs/textareas `text-sm` → `typographyClass.bodyInput` (16px, TMA iOS zoom fix); commit `a355f09`.

**TYPO-001 Phase 7 (automated gates):** grep `text-[Npx]` app UI → 1 escape (icon glyph `text-[18px]`); Merriweather → 0 in code; `modalSheetTitleClass` → `text-ui-heading`; Logo `text-7xl` frozen; `SettingsScreen` score/team `type="number"` → `bodyInput`; typecheck + client tests green. **Manual @375px TMA** — pending owner (Menu, Profile, Lobby, modals, connection banner, EnterName focus zoom).
