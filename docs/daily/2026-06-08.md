# MOVLI Master — 2026-06-08

**TYPO-001 wrap-up:** ESLint — 53 unused typography imports removed (Session C files); `typography.test.ts` `@theme` governance guard (`--text-ui-*`, no `--font-size-ui-*`); emoji avatar `text-sm` escape in `08-typography.mdc` + `TYPOGRAPHY_UNIFICATION.md`; deleted `debug-offline-add.png`, `.gitignore` `debug-*.png`.

**Lobby audit + fix prompts:** глибокий аналіз лоббі (client + server) — 12+ issues (TMA back, teamsLocked Redis, offline remove ghost, server readiness gap). Зібрано `docs/LOBBY_FIX_PROMPTS.md` — Phase 1–4 + Final Audit; оновлено `docs/INDEX.md`, `CURRENT_FOCUS`.

**Lobby Fix Phase 1 (done):** TMA back navigation (`SETTINGS`/`TEAMS`→`LOBBY`, `LOBBY`→`leaveRoom`); offline `REMOVE_OFFLINE_PLAYER` cleans teams; Redis restore `teamsLocked`; shared `buildTeamShells` for `LobbyScreen` + `TeamSetupScreen`; unit tests (`useTelegramBackButton`, `offlineGameActions`, `RoomManager`). Verify: `pnpm typecheck` green; client **134/134**; server **344/344**.

**Lobby Fix Phase 2 (done):** `@movli/shared` `deriveLobbyReadinessServer`; START_GAME server validation (`LOBBY_NOT_READY`); game-state guards (`INVALID_STATE` for lobby actions mid-game); `room:exists` no writer-only false positive; `room:join` only in `LOBBY` (`GAME_ALREADY_STARTED`); relay join parity. Verify: `pnpm build:shared && typecheck` green; shared **17/17**; server **355/355**.

**Lobby Fix Phase 3 (done):** optimistic settings rollback on `room:error`; `leaveRoom({ resetGameMode?: boolean })` for offline exit; single increment in `LobbyPlayModeBar`; `teamsExpanded` user toggle preserved; Start `disabled` when `!ready`; i18n (TeamSetup, TMA deep link, Settings game-mode hints); overfill warn-only documented; QR fail toast. Verify: `pnpm typecheck` green; client **138/138**.

**Lobby Fix Phase 4 (done):** `buildTeamShells.test.ts`, `TeamSetupScreen.test.tsx`; `socketHandlers.int` writer-only `room:exists`; E2E `lobby-team-builder.spec.ts` (`@core` + `@smoke`); `game-ui.ts` `joinTeam` / `expectLobbyReadiness`; multiplayer reconnect locator → `lobby-avatar-strip`. Prettier drift (Phase 1–3 files) → `pnpm verify` green. Client **142/142**; server **356/356**; E2E lobby `@core` green.

**Lobby Fix Final Audit (done):** повторна перевірка B1–B12 — усі ✅; architecture invariants OK; 0 нових 🔴. Verify: `pnpm build:shared && typecheck && verify` green; E2E `lobby|@core|@smoke` **33 passed / 3 skipped**. Deferred: manual TMA @375px (I-13), `LobbyScreen.test` act warnings (I-14). Оновлено `AUDIT_RESULTS` Block I, `CURRENT_FOCUS`, `LOBBY_FIX_PROMPTS` checklist.

**TYPO-001 Session D (Phase 7 visual QA):** Playwright computed-style audit @375px (Pixel 5) + code review + E2E `@smoke` mobile-chrome **7/7** green. **Critical regression found & fixed:** Tailwind v4 `@theme` used `--font-size-ui-*` instead of `--text-ui-*` — `text-ui-heading|label|body|system` utilities were not generated; all semantic typography fell back to 16px inherited body. Fix: `packages/client/src/styles.css` → `--text-ui-*`, `--leading-ui-*`, `--tracking-ui-*`. Post-fix audit: Logo 72px, CTA labels 10px, ScreenTitle 24px. Epic TYPO-001 **closed**.

### Phase 7 checklist @375px

| Screen | 375px light | 375px dark | Issues | Fixed |
|--------|-------------|------------|--------|-------|
| Menu | ✅ | ✅ | `text-ui-*` CSS missing → CTA labels 16px | `styles.css` |
| Modal vs Screen titles | ✅ | ✅ | headings 16px (same root cause) | `styles.css` |
| Profile + ProfileSettings | ✅ | ✅ | — | — |
| Lobby (player list) | ✅ | ✅ | — | — |
| SettingsScreen | ✅ | ✅ | — | — |
| Connection banner | ✅ | ✅ | — | — |
| Rules modal/screen | ✅ | ✅ | — | — |
| Themes (PAPER_LUXE + PREMIUM_DARK) | ✅ | ✅ | — | — |
| GameFlow (smoke) | ✅ | ✅ | E2E round path green | — |

**Static gates:** grep `text-[Npx]` app UI → 1 icon escape; `text-xs|text-sm` → 2 emoji avatar escapes; `pnpm verify` green; client **126/126**.

---

**Lobby glass chrome + start CTA (done):** `ScreenShell` sticky header/footer inside scroll; `.ui-app-header` / `.ui-app-footer`; `FixedBottomBar glass`; `LobbyStartPanel` — faded-red blocked + lock, neon snake when ready, toast-only validation hints. Docs: `TMA_LAYOUT.md` (Glass app chrome), `LOBBY_TEAM_BUILDER.md`, `UI_TOKENS.md`. Verify: layout + lobby tests green; `pnpm typecheck` green. Manual TMA @375px — pending owner.

**TMA header unification Phase 4 (done):** `MenuScreen` home — fixed icon bar + manual spacer → `ScreenShell` + sticky `AppHeader` (variant A); removed `--tma-fixed-header-height` / `data-fixed-header`; `--app-home-card-top` + `appHomeCardTopCss()`; `EnterNameSheet` overlay — `aria-hidden` / `pointer-events-none` on frozen chrome; Vitest in `MenuScreen.buttons.test.tsx`. Verify: `pnpm typecheck` green; client **185/185**; grep `fixed left-0` / `tma-fixed-header-height` → 0 in screens.

**TMA header unification Phase 3a (done):** `ProfileScreen`, `ProfileSettingsScreen`, `PlayerStatsScreen`, `MyDecksScreen`, `MyWordPacksScreen`, `LobbySettingsScreen` → `ScreenShell` + `AppHeader`; save/create → `FixedBottomBar`; ad-hoc `pt-safe-top` headers removed; `LobbySettingsScreen` browser back = TMA matrix (LOBBY/MENU). Verify: `pnpm typecheck` green; client **182/182**; grep gate 0 on Profile*/My*.

**TMA header unification Phase 3b (done):** `SettingsScreen`, `TeamSetupScreen`, `JoinInputScreen`, `RulesScreen`, `VSScreen`, `CountdownScreen`, `RoundSummaryScreen`, `ScoreboardScreen`, `GameOverScreen` → `ScreenShell` (+ `AppHeader` where nav chrome); footers → `FixedBottomBar`; `settings-close` test id preserved; `ImposterScreen` / `PlayingScreen` untouched. Header matrix updated. Verify: `pnpm typecheck` green; client **182/182**; grep `pt-safe-top` in screens → `ImposterScreen` only.

**TMA header unification Phase 2 (done):** `AppHeader` API — `title`, `onBack`, `showBackInBrowser`, `children`/`childRowHeightPx`, `tgChromeGutter`; CSS title/child rows + `data-tg-gutter` 80px; `LobbyScreen` refactor 1:1 (browser X back, TMA native spacer, settings only outside TMA online). Verify: `pnpm typecheck` green; client **182/182**.

**TMA header unification Phase 0 (done):** grep audit `packages/client/src/screens/**`; класифікація A–E; Header matrix у `TMA_LAYOUT.md` (20 `GameState` + defer surfaces); gaps — лише `LobbyScreen` на каноні A, `MenuScreen` fixed B, 7 ad-hoc `pt-safe-top` headers, `StoreScreen` missing safe top. Без TSX. Оновлено `CURRENT_FOCUS`, `CHANGELOG` [Unreleased].

**TMA header unification Phase 1 (done):** `tmaLayoutConstants.ts` + Vitest; `--app-page-header-height` у `styles.css` (fallback formula); `GlassAppHeader` `ResizeObserver` → inline measured px; `data-app-header` toast offset via measured height; docs `UI_TOKENS` + `TMA_LAYOUT` Constants. Без screen migration. Verify: `pnpm typecheck` + client tests.

---

**TYPO-001 Session C:** modals/menu/lobby/shared — `text-xs`/`text-sm` → semantic tokens; 17 files.

**TMA header unification Phase 6 (done):** full verification — grep gates 0; `pnpm build:shared && typecheck` green; client **202/202**; E2E `@smoke` **15 passed / 1 skipped**; code review checklist (automated) ✅; `SettingsScreen` → `AppHeader` `backTestId="settings-close"`; E2E `neuterTelegramMiniAppDetection` + `forceBrowserChromeMode`. Deferred: manual TMA @375px (J-2), `StoreScreen` safe inset (J-6). Оновлено `AUDIT_RESULTS` Block J, `CURRENT_FOCUS`, `CHANGELOG`.

**TMA header unification Phase 5 (done):** 88px content-safe floor (`--tma-content-top-floor`, `useTelegramApp` sync); `--tma-inset-top` CSS fallback chain; `resolveTelegramBackAction()` + `TELEGRAM_BACK_HIDDEN_STATES` (MENU, ENTER_NAME); `--tma-banner-top` for `ConnectionStatusBanner`; docs OMR vs Alias + computed heights. Verify: `pnpm typecheck` + client tests.

**TYPO-001 Session B:** `SettingsScreen` — legacy `text-xs`/`text-sm` → semantic tokens; client **126/126**.

**TYPO-001 Session A:** 5 screens — inputs → `typographyClass.bodyInput` (16px); commit `a355f09`.

**TYPO-001 Phase 7 (automated gates):** grep green; Settings number inputs → `bodyInput`.

**E2E lobby helper sync (CI fix):** `game-ui.ts` — `expectLobbyReadiness` uses blocked start shell (`aria-disabled`, `--blocked`) замість видаленого `lobby-start-validation`; `closeLobbySettings` → `expectLobbyScreen` (`lobby-start-panel`, offline-safe). Verify: `pnpm verify` green; E2E `@core` chromium **9/9**.
