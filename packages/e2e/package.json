{
  "name": "@movli/e2e",
  "version": "0.6.5",
  "private": true,
  "scripts": {
    "test": "node run-playwright.mjs",
    "test:extended": "node run-playwright.mjs -- --grep @extended",
    "test:locators": "node --test tests/helpers/game-ui.locators.test.mjs",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
