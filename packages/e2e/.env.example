# Copy to `packages/e2e/.env` (gitignored) if the default URL does not match your Postgres.
# Default in test-env.ts matches root docker-compose.yml (user: alias, password: alias_dev).
#
# E2E_DATABASE_URL=postgresql://alias:alias_dev@127.0.0.1:5432/alias?schema=public
