# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.turbo
*.local

# Environment (єдиний робочий файл у корені — `.env.prod`, у git лише `*.example`)
.env
.env.*
!.env.prod.example
!.env.*.example

# Package env files
packages/*/.env
packages/*/.env.*
!packages/*/.env.example
!packages/*/.env.*.example

# CI: local copy of deploy key must never be committed
.gha_deploy_key

# Локальні нотатки про VPS (IP, домени, топологія) — не в репозиторій
docs/VPS-INFRASTRUCTURE.md

# Prisma
packages/server/prisma/*.db

# Playwright (E2E artifacts)
packages/e2e/playwright-report/
packages/e2e/test-results/
packages/e2e/debug-*.png
packages/e2e/.e2e-auth.json

# Vitest coverage artifacts
packages/server/coverage/

# OS
nul

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
